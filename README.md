
## Running uvicorn

```
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

Or with bash syntax highlighting:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

## Running streamlit

```
streamlit run streamlit_app.py
```
